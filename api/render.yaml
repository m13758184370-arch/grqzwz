services:
  - type: web
    name: ai-resume-api
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn app.dev_server:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.12.0
